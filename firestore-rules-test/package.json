{
  "name": "firestore-rules-test",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cd .. && firebase emulators:exec --only firestore --project globalconsultorias 'node --test --test-concurrency=1 firestore-rules-test/*.rules.test.mjs'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase-tools": "^15.28.1"
  },
  "dependencies": {
    "firebase": "^12.16.0"
  }
}
